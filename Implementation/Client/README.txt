When adding assets to the Client Project, Ensure you add them to both:

Debug>>Resources
Clinet>>Resources

If there is an error within 'ClientAPI' or 'Client' can not find 'ClientAPI', simply Rebuild 'ClientAPI'. This error is because the .lib is either out of date, or wasn't synced correctly.